from setuptools import setup, find_packages


setup(name='hate',
      version='0.1',
      description='Hate speech detection',
      author='',
      packages=find_packages(),
        install_requires=[]
    )

   